
import { Steps } from 'nextra/components'
import { Callout } from 'nextra/components'

<Steps>

## Syntax
```js

import { ComponentMeta, ComponentStory } from '@storybook/react';
import * as React from 'react';
import { Button } from './Button';

// More on default export: https://storybook.js.org/docs/react/writing-stories/introduction#default-export
export default {
  title: 'Example/Button',
  component: Button,
  // More on argTypes: https://storybook.js.org/docs/react/api/argtypes
  argTypes: {
    backgroundColor: { control: 'color' },
  },
} as ComponentMeta<typeof Button>;

// More on component templates: https://storybook.js.org/docs/react/writing-stories/introduction#using-args
const Template: ComponentStory<typeof Button> = (args) => <Button {...args} />;

export const Primary = Template.bind({});
// More on args: https://storybook.js.org/docs/react/writing-stories/args
Primary.args = {
  primary: true,
  label: 'Button',
};

export const Secondary = Template.bind({});
Secondary.args = {
  label: 'Button',
};

export const Large = Template.bind({});
Large.args = {
  size: 'large',
  label: 'Button',
};

export const Small = Template.bind({});
Small.args = {
  size: 'small',
  label: 'Button',
};

```
## Storybook Configuration

The Storybook configuration for the Button component is defined in the file that exports the component, typically named ```Button.stories.js ```or similar. Here's an example of the configuration:
```js
import { ComponentMeta, ComponentStory } from '@storybook/react';
import * as React from 'react';
import { Button } from './Button';

export default {
  title: 'Example/Button',
  component: Button,
  argTypes: {
    backgroundColor: { control: 'color' },
  },
} as ComponentMeta<typeof Button>;

```
The ``` title ```  specifies the title of your story, and the component indicates which component this story is associated with. ``` argTypes```  can be used to customize the story's argument types

### Component Stories
#### Primary Button
This story demonstrates a ```primary button``` with a label ```Button```

```js
export const Primary = Template.bind({});
Primary.args = {
  primary: true,
  label: 'Button',
};
```
#### Secondary Button
This story demonstrates a ```secondary button``` with a label ```Button.```

```js
export const Secondary = Template.bind({});
Secondary.args = {
  label: 'Button',
};
```

#### Small Button
This story demonstrates a ```small button``` with a label ```Button```

```js
export const Small = Template.bind({});
Small.args = {
  size: 'small',
  label: 'Button',
};
```
#### Large Button

This story demonstrates a ```large button``` with a label ```Button.```

```js
export const Large = Template.bind({});
Large.args = {
  size: 'large',
  label: 'Button',
};
```

</Steps>
